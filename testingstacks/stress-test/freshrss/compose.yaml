services:
  freshrss:
    image: lscr.io/linuxserver/freshrss:latest
    container_name: freshrss
    restart: unless-stopped
    ports:
      - "8087:80"
    environment:
      - PUID=1000
      - PGID=1000
    volumes:
      - ./config:/config
