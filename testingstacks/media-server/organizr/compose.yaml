services:
  organizr:
    image: organizr/organizr
    container_name: organizr
    restart: unless-stopped
    ports:
      - "80:80"
    volumes:
      - ./config:/config
    environment:
      TZ: America/New_York
      PUID: 1000
      PGID: 1000
