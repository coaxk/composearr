services:
  requestrr:
    image: thomst08/requestrr:latest
    container_name: requestrr
    ports:
      - "4545:4545"
    volumes:
      - ./config:/root/config
    environment:
      - PUID=1000
      - PGID=1000
