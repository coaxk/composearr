services:
  web:
    image: nginx:1.21.6
    restart: unless-stopped
    ports:
      - "8080:80"
    environment:
      TZ: Australia/Sydney
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost"]
      interval: 30s
      timeout: 10s
      retries: 3
